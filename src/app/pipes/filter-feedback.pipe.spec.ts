import { FilterFeedbackPipe } from './filter-feedback.pipe';

describe('FilterFeedbackPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFeedbackPipe();
    expect(pipe).toBeTruthy();
  });
});
